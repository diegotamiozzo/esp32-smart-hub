[build]
  command = "npm run build"
  publish = "dist"

[dev]
  targetPort = 8080
  command = "npm run dev"
  framework = "#custom"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200